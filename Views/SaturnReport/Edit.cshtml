@using Microsoft.AspNetCore.Identity
@inject UserManager<ckl.Data.ApplicationUser> UserManager

@model IEnumerable<SaturnReport>
@{
    ViewData["Title"] = "Homepage";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="container">
    <div class="row">
        <div class="col-sm-6">
            <h2>Update</h2>
            <form asp-controller="SaturnReport" asp-action="Edit" method="post">
                @foreach (var saturnReport in Model)
                {
                    <div class="row">
                        <div class="col-md-4"></div>
                        <div class="form-group">
                            <label asp-for="@saturnReport.Title"></label>
                            <input asp-for="@saturnReport.Title" class="form-control" />
                            <span asp-validation-for="@saturnReport.Title" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="@saturnReport.Desciption"></label>
                            <input asp-for="@saturnReport.Desciption" class="form-control" />
                            <span asp-validation-for="@saturnReport.Desciption" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="@saturnReport.BodyText"></label>
                            <input asp-for="@saturnReport.BodyText" class="form-control" />
                            <span asp-validation-for="@saturnReport.BodyText" class="text-danger"></span>
                        </div>

                    </div>
                    <button asp-controller="SaturnReport" asp-action="Edit" asp-route-id="@saturnReport.Id" type="submit" class="btn btn-default">Update</button>
                }
            </form>
        </div>
    </div>
</div>
