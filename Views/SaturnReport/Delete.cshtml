@using Microsoft.AspNetCore.Identity

@inject SignInManager<ckl.Data.ApplicationUser> SignInManager
@inject UserManager<ckl.Data.ApplicationUser> UserManager

@model IEnumerable<SaturnReport>
@{
    ViewData["Title"] = "Homepage";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}



<div class="container">
    <h2>Delete User</h2>
    <div class="col-md-9">
        <div class="profile-content">
            <form asp-controller="SaturnReport" asp-action="Remove" method="post">

                <table class="bootstrap-table table">
                    <thead class="thead-dark">
                        <tr>
                            <th scope="col">Title</th>
                            <th scope="col">Desciption</th>
                            <th scope="col">Body</th>
                        </tr>
                    </thead>
                    @foreach (var report in Model)
                    {
                        <tr>
                            <td>@report.Title</td>
                            <td>@report.Desciption</td>
                            <td>@report.BodyText</td>
                            <td>
                                <a asp-controller="SaturnReport" asp-action="Edit" asp-route-id="@report.Id">Edit</a> |
                            </td>
                        </tr>
                    }
                </table>
                <button asp-action="Delete" asp-controller="SaturnReport" type="submit" class="btn btn-danger"> Delete</button>
            </form>
        </div>
    </div>
</div>